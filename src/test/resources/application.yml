server:
  port: 7900

spring:
  security:
    google:
      enabled: true
      authc:
        acceptable-time-skew-seconds: 300
        authorization-param-name: accessToken
        client-ids:
          - xxxx
          - yyyyy
        path-pattern: /login/google
        post-only: true
